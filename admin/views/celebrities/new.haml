.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:celebrities, :index)
      %li.active=link_to pat(:new), url(:celebrities, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:celebrity)
    .inner
      -form_for :celebrity, url(:celebrities, :create), :class => :form do |f|
        =partial "celebrities/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
